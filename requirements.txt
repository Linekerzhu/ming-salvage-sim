# 直接依赖：下限保证所需特性，上限堵住无界大版本升级断裂（G4.2 / tech-debt TD-002）。
# 上限策略：`<下一主版本`——允许补丁与小版本升级，拦住破坏性主版本跳跃。
# 锁定的可复现版本集见 requirements.lock（pip freeze 生成）。
agno[openai,sqlite]>=2.6.7,<3
debugpy>=1.8.20,<2
fastapi>=0.115.0,<1
pexpect>=4.9.0,<5
pillow>=10.3.0,<12  # >=10.3 规避 <10.3 的图像解析 CVE（SEC-007）
uvicorn[standard]>=0.32.0,<1
httpx>=0.27,<1
pydantic-settings>=2.14,<3
